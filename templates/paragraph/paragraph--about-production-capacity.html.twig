<section class="prodt_qulty_sec">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-md-6">
        <div class="detail">
          {{ content.field_image }}
        </div>
      </div>
      <div class="col-md-6">
        <div class="scroll_info">
          <div class="missie_block">
            {% for i in range(0, 1) %}
            <div class="prodt_detail">
              <h3>{{ content.field_about_title[i] }}</h3>
              {{ content.field_about_desp[i] }}
            </div>
            {% endfor %}
            <div class="btn-m">
              {{ content.field_link }}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>